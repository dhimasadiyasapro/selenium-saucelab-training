language: ruby
cache: bundler

rvm:
  - 2.4.1

before_install:
  - gem update --system
  - gem --version
install: gem install rails

before_script:
  - 'bundle update'
  - 'bundle install'

branches:
  only:
    - master

notifications:
  email:
    - dhimas@mokapos.com
